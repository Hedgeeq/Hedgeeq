<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Shop Interactive Guide</title>
<style>
  body {
    margin: 0;
    font-family: Arial, sans-serif;
  }
  #header {
    position: relative;
    height: 150px;
    background: url('background-placeholder.png') center/cover no-repeat;
    display: flex;
    align-items: flex-end;
    padding: 0 20px;
  }
  #desk {
    position: relative;
    width: 300px;
    height: 120px;
    background: url('desk-image-placeholder.png') center/contain no-repeat;
    z-index: 1;
  }
  #bell {
    position: absolute;
    left: 50%;
    bottom: 10px;
    width: 40px;
    height: 40px;
    cursor: pointer;
    transform: translateX(-50%);
    z-index: 3;
  }
  #character {
    position: absolute;
    left: 20px;
    bottom: 130px;
    width: 120px;
    height: 140px;
    z-index: 2;
    opacity: 0;
    transition: opacity 0.5s ease;
  }
  #chatbox, #sidepanel {
    opacity: 0;
    transition: opacity 0.5s ease;
  }
  #chatbox {
    position: absolute;
    left: 160px;
    bottom: 90px;
    width: 400px;
    height: 100px;
    background: #f5f5f5;
    border: 2px solid #ccc;
    padding: 10px;
    z-index: 2;
    font-size: 16px;
  }
  #sidepanel {
    position: absolute;
    right: 20px;
    bottom: 90px;
    width: 150px;
    height: 100px;
    background: #eee;
    border: 2px solid #ccc;
    padding: 10px;
    z-index: 2;
  }
  #closeBtn {
    position: absolute;
    top: 5px;
    right: 5px;
    background: #bbb;
    border: none;
    border-radius: 50%;
    width: 24px;
    height: 24px;
    cursor: pointer;
    font-weight: bold;
    display: none; /* hidden initially */
  }
</style>
</head>
<body>

<div id="header">
  <div id="desk"></div>
  <img id="bell" src="bell-icon-placeholder.png" alt="Bell" title="Click me to talk" />
  <img id="character" src="character-idle.gif" alt="Character" />
  <button id="closeBtn" title="Close interaction">×</button>

  <div id="chatbox">Welcome! Click a question to start.</div>
  <div id="sidepanel">
    <button class="question-btn">Who are you?</button>
    <button class="question-btn">What do you sell?</button>
    <button class="question-btn">How to buy?</button>
  </div>
</div>

<script>
  const bell = document.getElementById('bell');
  const character = document.getElementById('character');
  const chatbox = document.getElementById('chatbox');
  const sidepanel = document.getElementById('sidepanel');
  const closeBtn = document.getElementById('closeBtn');
  const questionButtons = document.querySelectorAll('.question-btn');

  function fadeIn(element) {
    element.style.opacity = '1';
  }
  function fadeOut(element) {
    element.style.opacity = '0';
  }

  // Initial state: hide character, chatbox, sidepanel, close button
  character.style.opacity = '0';
  chatbox.style.opacity = '0';
  sidepanel.style.opacity = '0';
  closeBtn.style.display = 'none';

  bell.addEventListener('click', () => {
    // Show interaction elements
    fadeIn(character);
    fadeIn(chatbox);
    fadeIn(sidepanel);
    closeBtn.style.display = 'block';
    bell.style.display = 'none';

    // Set character to popping up GIF then idle
    character.src = 'character-poppingup.gif';
    setTimeout(() => {
      character.src = 'character-idle.gif';
    }, 1000); // adjust duration to your GIF length
  });

  closeBtn.addEventListener('click', () => {
    // Hide interaction elements
    fadeOut(character);
    fadeOut(chatbox);
    fadeOut(sidepanel);
    closeBtn.style.display = 'none';
    bell.style.display = 'block';

    // Play going down animation, then idle hidden
    character.src = 'character-goingdown.gif';
    setTimeout(() => {
      character.style.opacity = '0';
      character.src = 'character-idle.gif'; // reset
    }, 1000);
  });

  // Chat interaction example
  questionButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      character.src = 'character-talking.gif';
      chatbox.textContent = 'Typing...';
      let text = '';
      if (btn.textContent === 'Who are you?') {
        text = 'I am your friendly shop assistant!';
      } else if (btn.textContent === 'What do you sell?') {
        text = 'I sell awesome, handmade goods.';
      } else if (btn.textContent === 'How to buy?') {
        text = 'Click the purchase page to place an order.';
      }
      // Typewriter effect
      let i = 0;
      chatbox.textContent = '';
      const interval = setInterval(() => {
        chatbox.textContent += text.charAt(i);
        i++;
        if (i > text.length) {
          clearInterval(interval);
          character.src = 'character-idle.gif';
        }
      }, 40);
    });
  });
</script>

</body>
</html>
